const { writeToken, removeToken } = require("../helpers");

describe("helpers", () => {
  describe(".writeToken", () => {
    it("should be a function with arity 2", () => {});
    it("should create the directory needed", () => {});
    it("should create the file requried with the token inside", () => {});
  });

  describe(".removeToken", () => {
    it("should be a function with arity 1", () => {});
    it("should unlink the file", () => {});
  });
});
